metadata:
  name: fetal-brain-3d-recon-argo-template
  generateName: fetal-brain-3d-recon-argo-template-
spec:
  templates:
    - name: fetal-brain-3d-recon
      inputs:
        artifacts:
          - name: input-dicom
            path: /var/monai/input
            s3:
              accessKeySecret:
                name: my-minio-cred
                key: accesskey
              secretKeySecret:
                name: my-minio-cred
                key: secretkey
              key: argo/inputs/artifacts/input-dicom
      outputs:
        artifacts:
          - name: output
            path: /var/monai/output
            s3:
              bucket: bucket1
              accessKeySecret:
                name: my-minio-cred
                key: accesskey
              secretKeySecret:
                name: my-minio-cred
                key: secretkey
              key: /var/monai/output
      metadata: {}
      container:
        name: ''
        image: fetalsvrtk/aide:map
        command:
          - /bin/bash
          - '-c'
        args:
          - python3 -u /opt/monai/app/app.py
        resources:
          limits:
            nvidia.com/gpu: '1'
  entrypoint: fetal-brain-3d-recon
  arguments: {}
